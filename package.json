{
  "name": "memory-card-game",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && surge"
  },
  "devDependencies": {
    "vite": "^2.9.7"
  },
  "dependencies": {
    "surge": "^0.23.1"
  }
}
